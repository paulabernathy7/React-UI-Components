import React from 'react';
import './Card.css';

const CardContent = () => {
    return (
        <div className="card-bottom">
        <p className ="card-content">Get Started with React</p>
        <p className="card-content">React makes it painless to make interactive UI's. Design simple views
        for each state in your application.</p>
        <a href="https://www.reactjs.org"></a>

        </div>
    )
};

export default CardContent; 